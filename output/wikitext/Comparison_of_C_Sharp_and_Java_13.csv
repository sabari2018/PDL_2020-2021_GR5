Java,C#
public int addOne(final int x) {     x++; // ERROR: a final variable cannot be reassigned     return x; }
public ArrayList addOne(final List list) {     list.add(1); // OK: it is still possible to modify a                  // final (reference type) variable     return list; },public int AddOne(in int x)  {     x++; // ERROR: a readonly parameter cannot be reassigned     return x; }
public List AddOne(in List list)  {     list.Add(1); // OK: it is still possible to modify a                  // readonly (reference type) parameter     return list; }
